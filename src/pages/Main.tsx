import { FC } from 'react';
import { Board } from '../components';

const Main: FC = () => {
    return (
        <section className="page">
            <Board />
        </section>
    );
};

export default Main;